# LostAndFound
PRoject
Features Implemented:
User Management
Register new users via API (POST /user/register)

Each item is associated with a registered user

Item Reporting
Report lost or found items (POST /items/post)

Submit title, description, and status ("lost" or "found")

View all lost items (GET /items/lost)

View all found items (GET /items/found)
